* `countNSXCores.json`
    * Calculate the sum of ESX cores (at levels like Datacenter, Cluster etc.), based on a Custom Property `CustomProperty|tk-isNSXenabled` that is added for example through membership in a custom group with criteria like "ESX related to NSX Transport Zone"  
